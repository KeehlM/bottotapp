
const defaultUserState = {
  studentList:[],
  isPending:true
};

function home(state = defaultUserState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

module.exports = home;
